query CartItemsList ($slugs: [String!]!) {
    products(slugs: $slugs) {
        data {
            slug
            title
            description
            image
            price
            firm
            capacity
        }
    }
}