query BlogInfo($slug: String!) {
    post (slug: $slug, include: ["tags"]) {
        primaryTag {
            slug
            name
        }
        featureImage
        title
        excerpt
        createdAt
        html
        tags {
            id
            name
            visibility
        }
    }
}